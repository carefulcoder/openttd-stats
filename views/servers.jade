extends layout

block content
    .page-header: h1 Running Server Instances
    table.table.table-bordered
        thead: tr
            th Server Name
            th Server Actions
        tbody
            each server, id in servers
                tr
                    td #{server.name}
                    td
                        .row-fluid
                            a.btn.span2 View Console
                            a.btn.btn-danger.span2(href="#{settings.uri}trains/kill/#{id}/") Kill Server
    a.btn.btn-success(href="#{settings.uri}trains/spawn") Spawn New Server